extends layout

block content 
    h1 Form
    form(method="POST" action="/new")
        label(for="messageUser") User
        input(type="text" id="messageUser" name="messageUser")
        label(for="messageText") Message Text
        textarea(id="messageText" name="messageText")
        button(type="submit") Submit
